# pdf-to-excel-converter
Tool to convert pdf's with a summary section and tables to excel. Minimal formatting is done. Combines Tables to a single sheet, if header is the same. Else, different tables go to diferent sheets.
